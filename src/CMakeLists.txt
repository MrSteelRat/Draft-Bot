# Projects
if(DOCBOTS_ENABLE_CPPSTD_DISCORDBOT)
    add_subdirectory(CppStdDiscordBot)
endif()
if(DOCBOTS_ENABLE_CPPSTD_TELEGRAMBOT)
    add_subdirectory(CppStdTelegramBot)
endif()
if(DOCBOTS_ENABLE_BOTS_SERVER)
    add_subdirectory(BotsServer)
endif()
